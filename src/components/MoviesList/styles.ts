import { StyleSheet } from 'react-native';
import { hp, wp } from '../Responsive';


export default StyleSheet.create({
 
  listContainer: {
    marginTop: 10,
  },
  listHeading: {
    fontSize: 20,
    fontWeight: 'bold',
    paddingBottom: 10,
  },
});
